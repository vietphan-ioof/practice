const x = 1
let y = 5

console.log(x, y)
y+=10
console.log(x, y)
y = 'sometext'
console.log(x, y)
//x = 4 

//arrays
console.log('------arrays-----')

const t = [1, -1, 3]
t.push(5)
console.log(t.length)
console.log(t[1])

t.forEach(value => {
	console.log(value)
})


const t2 = t.concat(5)
console.log(t)  // [1, -1, 3] is printed
console.log(t2) // [1, -1, 3, 5] is printed

//map method
console.log('------map-----')

const t3 = [1, 2, 3]
const m1 = t3.map(value => value * 2)
console.log(m1)

const m2 = t3.map(value => '<li>' + value + '<li>')
console.log(m2)

//destructing argument
console.log('------destructing argument-----')

const t4 = [1, 2, 3, 4, 5]

const [first, second, ...rest] = t4
console.log(first, second)
console.log(rest)

//Objects
console.log('------objects-----')

const object1 = {
	name: 'Benny Bronco',
	age: 420, 
	education: 'Broke',
}

const object2 = {
	name: 'ROd Wave',
	level: 9001, 
	size: 'scary',
}

const object3 = {
	name: {
		first: 'Dan', 
		last: 'Abramov',
	},
	grades: [2, 2, 2, 2],
	department: 'barnacle barnes university',
}

console.log(object1.name)
const fieldName = 'age'
console.log(object1[fieldName])

//add properties to object

object1.address = 'Jeblinksi'
object1['secret number'] = 12345

console.log(object1)



console.log('------Functions-----')

const sum = (p1, p2) => {
	console.log(p1)
	console.log(p2)
	return p1+p2
}

const result = sum(1, 5)
console.log(result)





























