# put general exception on the bottom mostly


try:
    f = open('testfile.txt')
    var = bad_var
except FileNotFoundError as e:
    print(e)
except Exception as e:
    print(e)
else:
    # executes function it runs if there is no issue.
